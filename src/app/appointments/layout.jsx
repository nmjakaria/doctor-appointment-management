export const metadata = {
  title: "All Appointments",
  description: "Search and filter available doctor appointments by specialist names, hospital locations, or medical expertise.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}