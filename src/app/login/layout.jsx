export const metadata = {
  title: "Login",
  description: "Sign in to your DocAppoint account using your credentials, Google, or GitHub securely.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}