export const metadata = {
  title: "Register New Account | DocAppoint",
  description: "Create a verified account on DocAppoint to start booking and managing your doctor appointments instantly.",
};

export default function RegisterLayout({ children }) {
  return <>{children}</>;
}